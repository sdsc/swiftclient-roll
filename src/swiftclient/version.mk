ifndef ROLLPY
  ROLLPY = python
endif

ifndef PYVERSION
  PYVERSION = 2.6
endif

NAME           = sdsc-swiftclient_py$(PYVERSION)
VERSION        = 3.4.0
RELEASE        = 3
PKGROOT        = /opt/swiftclient

SRC_SUBDIR     = swiftclient
